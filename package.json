{
  "name": "pnpm-changesets",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "publishConfig": {
    "directory": "dist"
  },
  "scripts": {
    "xxx": "pnpm nx affected --target=publish:custom --base=master --head=HEAD",
    "version": "changeset version",
    "changeset": "changeset",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Milan Mladek",
  "license": "ISC",
  "devDependencies": {
    "@changesets/changelog-git": "^0.2.1",
    "@changesets/cli": "^2.28.1",
    "prettier": "^3.5.3",
    "nx": "21.1.2"
  }
}